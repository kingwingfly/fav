WHITESPACE = _{ " " }

video = { ^"videos" | ^"video" | ^"v" }
list = { ^"list" | ^"l" }

target = { video | list }

relation = { ^"in" | "=" }

id = { ASCII_DIGIT+ | (^"BV" ~ ASCII_ALPHANUMERIC+) }

query = { SOI ~ target ~ relation ~ id ~ EOI}
